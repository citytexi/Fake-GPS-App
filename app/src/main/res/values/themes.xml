<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.Fakegps" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Allows us to override night specific attributes in the
         values-night folder. -->
    <style name="NightAdjusted.Theme.Fakegps" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- The final theme we use -->
    <style name="Theme.Nia" parent="NightAdjusted.Theme.Fakegps" />

    <style name="NightAdjusted.Theme.Splash" parent="Theme.SplashScreen">
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">true</item>
    </style>

    <style name="Theme.Fakegps.Splash" parent="NightAdjusted.Theme.Splash">
<!--        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>-->
        <item name="postSplashScreenTheme">@style/Theme.Nia</item>
    </style>

</resources>